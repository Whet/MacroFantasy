package com.mygdx.game.components.primitive;

import com.badlogic.ashley.core.Component;

public class RemoveComponent extends Component {

	// Empty component that every entity should have to remove it from the engine
	// Engine removeAllEntities doesn't appear to work and just hangs but removing entities one at a time works
	
}
